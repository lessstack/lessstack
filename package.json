{
  "name": "witb",
  "version": "0.1.0",
  "private": "true",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "apps/*",
      "libs/*"
    ]
  },
  "scripts": {
    "lint": "lerna exec -- yarn lint"
  },
  "resolutions": {
    "semver-regex": ">=3.1.3",
    "trim-newlines": ">=3.0.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
