{
  "name": "witb",
  "version": "0.1.0",
  "private": "true",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "lint": "lerna exec -- yarn lint"
  },
  "resolutions": {
    "json-schema": "^0.4.0",
    "semver-regex": ">=3.1.3",
    "trim-newlines": ">=3.0.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
