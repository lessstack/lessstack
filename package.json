{
  "name": "lessstack",
  "version": "0.1.6",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@lessstack/eslint-config": "workspace:*",
    "eslint": "^8.2.0",
    "prettier": "^2.4.1",
    "turbo": "^1.6.2"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@7.5.0"
}
